# 이 가지는 **안드로이드 맞춤법 키보드**다

한국어 맞춤법·띄어쓰기를 실시간으로 고쳐 주는 안드로이드 키보드(IME)와 그 AI 교정 서버.

자세한 내력은 `docs/HANDOFF.md` 에 있다. **처음이면 그걸 먼저 읽어라.**

## 폴더

    core/       교정 엔진. 순수 코틀린, 안드로이드에 안 기댄다
    keyboard/   안드로이드 키보드(IME)
    server/     AI 교정 서버 (Cloudflare Worker + D1)
    tools/      사전 굽는 도구, 로컬 검사 스크립트
    docs/       HANDOFF.md

## 가지 — 조심할 것

이 저장소에는 **서로 상관없는 프로젝트가 가지로 같이** 들어 있다.

    claude/auto-spell-spacing-checker-r4ymq5   맞춤법 키보드 ← 지금 여기
    claude/deaf-noise-alert-app-m387hq         청각장애인용 소음 알림 앱 (**기본 가지**)

- **두 가지를 합치지 마라.** 소음 알림 앱의 `app/` 을 이 가지에서 걷어 냈으므로,
  이 가지를 기본 가지로 합치면 **그 앱이 진짜로 지워진다.** 합칠 물건이 아니다.
- 사용자가 허락하지 않는 한 **다른 가지에 밀지 마라.**
- 기본 가지가 소음 알림 쪽이라 **GitHub Actions 수동 실행 버튼이 이 가지 워크플로에는
  안 뜬다.** 배포를 손으로 돌리려면 성공한 실행을 Re-run 해야 한다.

## 윈도우 데스크톱 프로그램은 여기서 하지 않는다

같은 엔진(`core/`)을 쓰는 윈도우용 프로그램을 따로 만들고 있다. 그건 **`claude/` 로
시작하는 별도 가지에서** 하기로 했다. 이 가지에 `desktop/` 을 만들지 마라 —
커밋이 섞이고 APK 빌드가 헛돈다.

윈도우 얘기가 나오면 그쪽 가지/세션에서 하라고 알려 줘라.

## 빌드와 검사

**`./gradlew` 가 안 돈다.** `dl.google.com` 이 막혀 있어 안드로이드 그레이들 플러그인을
못 받는다. 대신 이걸 써라:

    tools/localcheck/test-core.sh           코어 시험 226개, 1초
    tools/localcheck/typecheck-keyboard.sh  키보드 타입 검사, 30초

**밀기 전에 반드시 둘 다 돌려라.** 확인 안 하고 밀어서 CI 를 세 번 연속 깨뜨린 적 있다.
APK 가 실제로 빌드되는지는 밀어 본 뒤 CI(`build-apk.yml`)로만 확인된다.

## 돈

AI 교정은 구글 Gemini API 를 쓴다. **사용자 돈이 나간다.**

- 값이 드는 일은 **돌리기 전에 얼마 나가는지 숫자로 말하고 허락을 받아라.**
- **자동으로 도는 것을 만들지 마라.** 모델 겨루기를 자동 실행으로 걸어 두었다가
  크레딧 8,000원어치가 날아가고 앱이 멈춘 적 있다.
- `server/bench/RUN` 은 실제로 측정할 때만 만들고, 끝나면 지워라.
