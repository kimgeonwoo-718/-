# 한국어 맞춤법·띄어쓰기 교정기

교정 엔진 하나에 **얼굴이 둘** 붙어 있다.

    core/       교정 엔진 — **둘이 공유한다.** 순수 코틀린, 안드로이드에 안 기댄다
    keyboard/   안드로이드 키보드(IME)
    server/     AI 교정 서버 (Cloudflare Worker + D1) — 안드로이드 쪽이 쓴다
    desktop/    윈도우 데스크톱 프로그램 (없으면 아직 안 만든 것이다)
    tools/      사전 굽는 도구, 로컬 검사 스크립트
    docs/       HANDOFF.md — 자세한 내력. 처음이면 **먼저 읽어라**

---

## ⚠ 한 세션은 한쪽만 건드린다

사용자가 무엇을 말했는지 보고 네 담당을 정해라. **가지 이름으로 판단하지 마라** —
세션마다 새로 생기므로 믿을 것이 못 된다.

| 사용자가 말한 것 | 네 담당 | 고칠 곳 |
|---|---|---|
| 키보드, 폰, APK, 천지인, AI 교정, 서버, 번역, 요금·한도 | **안드로이드** | `keyboard/`, `server/` |
| 윈도우, PC, 데스크톱, exe, 트레이, 클립보드, 단축키 | **윈도우** | `desktop/` |

- **반대쪽을 건드리지 마라.** 고쳐야 할 이유가 보여도 **사용자에게 먼저 말해라.**
- **애매하면 물어라.** 한 번 묻는 값이 잘못 건드리는 값보다 싸다.
- **한 가지에서 양쪽을 같이 고치지 마라.** 커밋이 섞여 되돌리기 어려워진다.

## `core/` — 제일 조심할 곳

양쪽이 같은 엔진을 쓴다. 그래서 여기를 고치면 **둘 다 영향을 받는다.**

- 윈도우 일을 하는 중이면 `core/` 는 **읽기만** 해라.
- 정말 고쳐야 하면 **사용자에게 먼저 말하고**, 고친 뒤 `tools/localcheck/test-core.sh`
  (시험 226개)를 반드시 돌려라.

---

## 빌드와 검사

### 안드로이드 쪽

**`./gradlew` 가 안 돈다.** `dl.google.com` 이 막혀 있어 안드로이드 그레이들 플러그인을
못 받는다. 대신:

    tools/localcheck/test-core.sh           코어 시험 226개, 1초
    tools/localcheck/typecheck-keyboard.sh  키보드 타입 검사, 30초

APK 가 실제로 빌드되는지는 밀어 본 뒤 CI(`build-apk.yml`)로만 확인된다.

### 윈도우 쪽

`desktop/` 은 **안드로이드 빌드와 갈라 놓은 별도 Gradle 빌드**다(자기 `settings.gradle.kts`
를 쓴다). 그래서 `dl.google.com` 없이 **여기서 그냥 돈다**:

    gradle -p desktop test
    gradle -p desktop fatJar            → desktop/build/dist/SpellDesktop.jar
    gradle -p desktop packageWindows    → .exe (윈도우에서만)

컨테이너는 화면이 없다. **Swing 창을 못 띄운다.** 로직과 UI 를 갈라 놓고 로직만 시험해라.

### 둘 다 해당

**밀기 전에 자기 쪽 검사를 반드시 돌려라.** 확인 안 하고 밀어서 CI 를 세 번 연속
깨뜨린 적 있다.

---

## 돈

AI 교정은 구글 Gemini API 를 쓴다. **사용자 돈이 나간다.**

- 값이 드는 일은 **돌리기 전에 얼마 나가는지 숫자로 말하고 허락을 받아라.**
- **자동으로 도는 것을 만들지 마라.** 모델 겨루기를 자동 실행으로 걸어 두었다가
  크레딧 8,000원어치가 날아가고 앱이 멈춘 적 있다.
- `server/bench/RUN` 은 실제로 측정할 때만 만들고, 끝나면 지워라.
- **AI 는 사용자가 누를 때만 불러라.** 치는 동안 자동으로 부르는 코드를 넣지 마라 —
  하루치가 몇 분 만에 날아간다. 온디바이스 교정(실시간·전체교정 둘 다)은 공짜다.
- 윈도우 쪽에서 서버를 쓸 때 **설치 ID 를 커밋하지 마라.** 공개 저장소라 누구나 꺼내
  사용자 돈으로 AI 를 쓴다. 로컬 설정에만 두어라.

## 가지

사용자가 허락하지 않는 한 **네게 주어진 가지 말고 다른 곳에 밀지 마라.**
윈도우 작업을 앱 가지에, 앱 작업을 윈도우 가지에 올리지 마라.

## 양쪽이 동시에 돌 수 있다

안드로이드 세션과 윈도우 세션이 같은 때에 따로 돌아간다. 폴더가 안 겹쳐서 대개는
부딪히지 않지만, **다음 셋은 양쪽이 같이 건드릴 수 있으니 조심해라:**

| 파일 | 규칙 |
|---|---|
| `core/` | 위의 "제일 조심할 곳" 그대로. 고치기 전에 사용자에게 말해라 |
| `docs/HANDOFF.md` | **안드로이드 쪽 기록이다.** 윈도우 일은 `desktop/README.md` 에 적어라 |
| `CLAUDE.md` | 고치기 전에 사용자에게 말해라. 양쪽 세션이 다 읽는 문서다 |

나머지(`keyboard/`, `server/` vs `desktop/`)는 서로 안 겹치니 마음 놓고 작업해라.
