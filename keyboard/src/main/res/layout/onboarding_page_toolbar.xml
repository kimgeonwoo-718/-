<?xml version="1.0" encoding="utf-8"?>
<!--
  안내 2쪽: 자판 위 도구 줄. 첫 화면 '사용법' 칸의 그림(ToolbarGuideView)을 그대로 쓴다 —
  동그라미를 누르면 막대기가 그 자리를 가리키며 설명이 뜬다.

  모든 쪽이 같은 뼈대다: 위쪽 남는 자리 한가운데에 흉내 화면을 크게 두고, 그 밑에 제목과 설명을
  가운데 맞춤으로 — 버튼 바로 위라 눈이 그림 → 글 → 버튼 순으로 내려온다. 처음엔 제목을 맨 위에
  두었더니 키 큰 폰에서 그림 밑이 휑하게 비었다. 화면이 작으면 통째로 스크롤된다.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="20dp"
        android:paddingBottom="4dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_marginVertical="8dp"
                android:background="@drawable/bg_card"
                android:orientation="vertical"
                android:padding="16dp">

                <com.spellkeyboard.ko.ToolbarGuideView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" />
            </LinearLayout>
        </FrameLayout>

        <TextView
            style="@style/GuideTitle"
            android:layout_marginTop="20dp"
            android:gravity="center"
            android:text="@string/guide2_title" />

        <TextView
            style="@style/GuideSub"
            android:gravity="center"
            android:text="@string/guide2_sub" />
    </LinearLayout>
</ScrollView>
