<?xml version="1.0" encoding="utf-8"?>
<!--
  안내 2쪽: 자판 위 도구 줄. 첫 화면 '사용법' 칸의 그림(ToolbarGuideView)을 그대로 쓴다 —
  동그라미를 누르면 막대기가 그 자리를 가리키며 설명이 뜬다.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="24dp"
        android:paddingTop="8dp"
        android:paddingBottom="16dp">

        <TextView style="@style/GuideTitle" android:text="@string/guide2_title" />
        <TextView style="@style/GuideSub" android:text="@string/guide2_sub" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:background="@drawable/bg_card"
            android:orientation="vertical"
            android:padding="16dp">

            <com.spellkeyboard.ko.ToolbarGuideView
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />
        </LinearLayout>
    </LinearLayout>
</ScrollView>
